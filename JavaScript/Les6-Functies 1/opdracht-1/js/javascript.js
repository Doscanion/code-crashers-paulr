function calcAverage(x, y) {
    if (Number.isInteger(x + y) == false) {
        console.log('fout');
    } else {
        console.log(x + y);
    }
}

calcAverage(25, 16);
calcAverage(15, 18);
calcAverage('test', 'tset');
